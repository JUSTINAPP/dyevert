import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // This project is designed to live at dyevert.com/postcodecolour
  // To deploy at that subpath, uncomment the line below and ensure
  // all internal <Link href="..."> values are relative (they are).
  // basePath: '/postcodecolour',
};

export default nextConfig;
